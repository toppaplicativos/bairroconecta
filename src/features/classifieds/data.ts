import {
  classifiedAds as legacyClassifiedAds,
  classifiedCategories as legacyClassifiedCategories,
} from '@/lib/data';

/** Transitional classifieds boundary for the future Supabase repository. */
export const classifiedAds = legacyClassifiedAds;
export const classifiedCategories = legacyClassifiedCategories;
